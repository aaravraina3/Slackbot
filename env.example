# Slack App Configuration
# Get these values from your Slack app settings at api.slack.com

# Bot User OAuth Token (starts with xoxb-)
# This is what your bot uses to send messages and interact with Slack
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# App-Level Token (starts with xapp-)
# This is used for Socket Mode (real-time communication)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Signing Secret
# Used to verify that requests are coming from Slack (security)
SIGNING_SECRET=your-signing-secret-here

# Optional: Bot Configuration
# These are custom settings for your bot
BOT_NAME=Generate BSCI Bot
BOT_EMOJI=:robot_face:
DEFAULT_CHANNEL=#general

# Optional: Database (if you want to store data)
# DATABASE_URL=sqlite:///bot_database.db

# Optional: Logging Level
# DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Development Mode
# Set to true for development, false for production
DEVELOPMENT_MODE=true
